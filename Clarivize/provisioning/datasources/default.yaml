# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0


apiVersion: 1

datasources:
  - name: Prometheus
    uid: webstore-metrics-cvz
    type: prometheus
    url: http://prometheus-cvz:9091
    editable: true
    isDefault: true
    jsonData:
      # Align Prometheus data source time interval with the OTel SDKs' export interval (see OTEL_METRIC_EXPORT_INTERVAL)
      timeInterval: "60s"
      exemplarTraceIdDestinations:
        - datasourceUid: webstore-traces-cvz
          name: trace_id

        - url: http://localhost:16687/jaeger/ui/trace/$${__value.raw}
          name: trace_id
          urlDisplayLabel: View in Jaeger UI
